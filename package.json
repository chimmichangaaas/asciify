{
  "name": "figma-ascii-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Figma plugin that turns images, GIFs, and videos into ASCII art on the canvas.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "gifenc": "^1.0.3",
    "gifuct-js": "^2.1.2"
  }
}
